# Multi-resource terminal views

Resources are dynamically discovered and users persist a multi-selection. The TUI provides detail, cluster, and resource aggregate views; cluster and resource aggregates sort by usage/request while detail defaults to name order. Tables paginate, search, and use terminal display width so ANSI colour codes cannot shift column separators.
