# New alias resource selection

Adding an alias begins with a keyboard selection from discovered resources. Only after selecting the underlying resource does the editor collect the alias name, exact label and annotation conditions, and display unit. Editing an existing rule remains a direct form.

This supersedes ADR 0007 only for new-rule resource entry. It prevents typos in the source resource while retaining direct condition authoring without a watched node.
