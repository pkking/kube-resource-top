# Alias metadata edit selection

In an existing alias editor, pressing `Enter` while the Labels or Annotations field is focused reopens the corresponding observed-metadata multi-select. Existing selected conditions are preselected, and confirming returns to the same field.

This extends ADR 0010's metadata selection to edits while retaining direct text entry for exceptional values.
