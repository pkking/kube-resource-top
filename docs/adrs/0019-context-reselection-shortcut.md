# Context reselection shortcut

Pressing `c` reopens the context selector while the TUI is running. The selector probes every discovered context, preserves prior selections until changed, persists the resulting selection, then restarts the watches on confirmation.
